[pytest]
<<<<<<< HEAD
# Directory where tests are located
testpaths = tests

# Patterns to locate test files, classes, and functions
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# CLI arguments to run by default
# -v: verbose output
# --strict-markers: prevents typos in custom markers
# --tb=short: keeps traceback short for readability
addopts = -v --strict-markers --tb=short

# Custom markers for categorizing tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    auth: marks tests related to authentication
    integration: marks tests that require external services or database

# Ignore specific warnings that might clutter the output (common in Flask/SQLAlchemy)
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning

# Environment variables for testing (requires pytest-env plugin)
env =
    FLASK_ENV=testing
    DATABASE_URL=sqlite:///:memory:
    JWT_SECRET_KEY=test-secret-key
=======
minversion = 6.0
addopts = -ra -q
testpaths = tests
>>>>>>> main
